

plugins {
    id 'java'
}

group = 'com.relaxingleg'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    version = '5.0.0-beta.24'
    implementation("net.dv8tion:JDA:5.0.0-beta.19")  // replace $version with the latest version
        // Optionally disable audio natives to reduce jar size by excluding `opus-java`
        // Gradle DSL:
        // exclude module: 'opus-java'
        // Kotlin DSL:
        // exclude(module="opus-java")

}

